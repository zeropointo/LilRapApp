name: lil_rap_app
description: A lil list of rappers

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  http: ^0.13.3
  shared_preferences: ^2.0.7
  get_it: ^7.2.0
  equatable: ^2.0.3
  dartz: ^0.10.0
  uuid: ^3.0.4
  connectivity_plus: ^1.1.0
  cached_network_image: ^3.1.0
  rxdart: ^0.27.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^5.0.15
  build_runner: ^2.1.2

flutter:
  uses-material-design: true
